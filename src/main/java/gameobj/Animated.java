/**
 * Interface for implementing animated objects.
 */
public interface Animated {
    /**
     * Change the object's sprite (changing sprite attribute) for the animation, 
     * as is required given the circumstances.
     */
    public void cycleAnimation();
}